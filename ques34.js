const pizza = ['pepperoni', 'fajita', 'thin crust'];
for (let index = 0; index < pizza.length; index++) {
    const element = pizza[index];
    console.log(`i really love ${element} pizzza!`);
}
console.log("i can eat pizza every day if it is good");
export {};
